# Zig build outputs
zig-out/
.zig-cache/
# Out-of-tree generator workspace + scratch builds (regenerate from upstream)
build/
# machine-specific (regenerated by tools/tcltest.sh)
vendor/tcl/tclConfig.sh
