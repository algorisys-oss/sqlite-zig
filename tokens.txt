# tokens.txt — ACTUAL per-prompt token usage
#
# REAL figures from the Claude Code session transcript (API `usage` per
# assistant step), NOT estimates. Regenerate with:
#   python3 tools/token_usage.py          (table)
#   python3 tools/token_usage.py --tsv     (machine-readable)
#
# A "turn" = one human prompt + ALL assistant work it triggered (every tool
# round-trip), up to the next prompt. TOTAL = input + cache_creation +
# cache_read + output. cache_read dominates (context re-read each step).
# Follow-ups sent mid-turn are queued into that turn, so their cost folds in.
# The currently-running turn isn't fully recorded until it ends.
#
# columns: start  end   input  cache_cr  cache_rd  output  ->  TOTAL  (cumulative)

[2026-06-24] 23:53:30-23:56:06  in=14937  cr=58608   rd=547112    out=26658  ->  TOTAL=647315     (cum=647315)
    prompt: Set up project (migrate SQLite C->Zig); create CLAUDE.md + plan.md.
    folded: count total tokens in project root; save per-prompt token count.

[2026-06-24] 23:59:22-00:13:08  in=3918   cr=115193  rd=6570937   out=98286  ->  TOTAL=6788334    (cum=7435649)
    prompt: Start Phase 0; keep track of things so we can continue later.
    folded: also track start/end time of each prompt (+ update CLAUDE.md).
    note:   out-of-tree generation, vendoring, build.zig, functional test gate.

[2026-06-25] 00:13:32-00:18:10  in=2381   cr=51385   rd=3428512   out=41800  ->  TOTAL=3524078    (cum=10959727)
    prompt: Draft a README.md and indicate how to verify/test.
    folded: track ACTUAL token usage per prompt, not just prompt tokens
            -> wrote tools/token_usage.py; tokens.txt now reflects real data.

[2026-06-25] 00:19:04-00:25:16  in=2406   cr=64866   rd=5055426   out=63061  ->  TOTAL=5185759    (cum=16145486)
    prompt: What's next? -> first real port: random.c -> src/random.zig
            (+ src/chacha.zig); proved swap mechanism; RFC 7539 vector check.

[2026-06-25] 00:27:02-(in progress)  in=4564  cr=147260  rd=10095783  out=92412  ->  TOTAL=10340019  (cum=26485505)
    prompt: Wire up the TCL testfixture suite (vendor tcl headers, no sudo).
    folded: git init and commit; park for today, continue tomorrow.
    note:   figures are through the last recorded step; re-run the script next
            session to capture this turn's tail.

[2026-06-26] 09:56:32-13:30:06 (in progress)  10 turns  ->  TOTAL=329932069  (cum=356417574)
    elapsed: ~3h33m wall clock so far (turn 10 = current, partial).
    prompt: Free for 8 hours; do the SQLite C->Zig migration (full autonomy,
            uninterrupted, spawn as many agents as needed). Also: test against
            SQLite's test cases AND port test cases to Zig; build a sample blog
            DB + a Zig script to create it; document decisions; log time+tokens.
    note:   Marathon agent-parallelized session. Ported 24 modules to Zig this
            session (25 total): random+hash+bitvec+rowset+fault+mem1+complete+
            memjournal+fts3_hash+utf+os+fts3_porter+fts3_tokenizer1+fts3_unicode+
            carray+table+fts3_unicode2+threads+mutex_noop+mem5+stmt+mutex+
            vdbetrace+legacy+pcache. Built the comptime-config foundation +
            ground-truth offset tooling (tools/offsets.c + gen_layout.sh ->
            src/c_layout.zig, 91 entries) so build-divergent core structs
            (Mem/sqlite3/Sqlite3Config/Vdbe/PgHdr) are mirrored provably-correctly.
            Caught a real optimizer bug (extern const vs var on a mutable global).
            Added a Zig-native engine test suite (test/engine_test.zig) + a sample
            blog DB (sampledata/, `zig build sample`). Scaled via background
            sub-agent waves; orchestrator owns shared files + validation + commits.
            Turn 10 is in progress; re-run the script next session for its tail.

[2026-06-27] 12:12:54-17:4x (in progress)  13 turns  ->  TOTAL=444033445  (cum=800451019)
    elapsed: ~5h+ wall clock so far (final turn in progress).
    prompt: Reconnect after a drop ("got disconnected"); continue the autonomous
            migration without stopping until the goal is reached.
    note:   Ported 18 modules this session (63 -> 81): pragma, status, btmutex,
            dbpage, dbstat, memdb, malloc, expr, whereexpr, wherecode, where,
            select, mutex_unix, main, ctime, global, treeview, os_unix. THE ENTIRE
            ACTIVE LINUX CORE ENGINE IS NOW ZIG: global config + ctype tables ->
            os_unix VFS (all file I/O) -> pager/wal -> btree -> vdbe -> tokenize/
            resolve -> expr/where*/select codegen -> func/json/window -> main API.
            Cleared the two long-deferred keystones: global.c (sqlite3Config
            byte-exact 440/488 + ctype/opcode tables byte-verified) and ctime.c
            (C-probed option lists). Skipped notify.c (inactive). Fixed a real
            pre-existing crash (sqlite3FinishCoding schema_cookie). Per-module
            integration bugs (~15 total) all from recurring classes: wrong
            hardcoded constants (P4_/SRT_/SF_/TF_/SQLITE_DYNAMIC/WO_), C int/i16
            fields read at wrong width, char[]-symbol address-is-data, inline-array
            vs pointer, struct bitfield positions, macros-as-extern-fns. Big
            modules (where/main/global/os_unix/mutex) drafted by parallel background
            agents that self-validated -> integrated with 0-to-minimal fixes;
            orchestrator owns offsets+integration+validation+commits. Remaining is
            NON-core: optional extensions (fts*/fts5/rtree/icu/session/rbu),
            other-platform (os_win/mutex_w32), inactive stubs (mem0/2/3/notify/
            os_kv), generated (parse.c/opcodes.c). Re-run script next session for
            this turn's tail.

# Session actual total so far (cumulative): 800,451,019 tokens.
